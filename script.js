// script.js

// Al momento, questo file può rimanere vuoto o contenere
// codice JavaScript semplice se necessario in futuro.

// Esempio: Log per confermare che lo script è caricato
console.log("Script caricato correttamente.");

// Esempio: Potresti aggiungere qui in futuro interazioni
// come animazioni allo scroll, validazione di form (se ne aggiungi),
// o chiamate API (se l'app avesse una demo web).

// Funzione di esempio per evidenziare il link attivo (alternativa al CSS puro)
// function setActiveLink() {
//     const currentPage = window.location.pathname.split("/").pop() || "index.html";
//     const navLinks = document.querySelectorAll('header nav ul li a');

//     navLinks.forEach(link => {
//         if (link.getAttribute('href') === currentPage) {
//             link.classList.add('active');
//         } else {
//             link.classList.remove('active'); // Rimuovi se già presente da HTML/CSS
//         }
//     });
// }

// window.addEventListener('DOMContentLoaded', setActiveLink); // Chiama la funzione quando il DOM è pronto